import { View, Text } from "react-native";

const Dripsy = () => {
  return (
    <View style={{ display: "flex", flexDirection: "row" }}>
      <Text>TODO</Text>
    </View>
  );
};

export default Dripsy;
